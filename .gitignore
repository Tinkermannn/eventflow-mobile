# Node.js
# ========================
# node_modules
node_modules/
# dependencies
/npm-debug.log*
/.npm/
/.yarn/

# testing
/coverage/
/.nyc_output/

# production
/build/
/dist/
/tmp/

# environments
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# misc
*.log
*.pnpm-debug.log*
.DS_Store
Thumbs.db

# TypeScript
# ========================
# Output of TypeScript compilation
*.js
*.js.map
*.d.ts

# VS Code
# ========================
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# OS and Editor files
# ========================
.idea/             # JetBrains IDEs (IntelliJ, WebStorm, etc.)
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace
*.bak
*.swp
*~
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Prisma
# ========================
prisma/migrations/
!prisma/migrations/                  # Uncomment if you want to keep migrations in git
prisma/*.sqlite                     # SQLite database files
*.db                                # General database files (e.g., for SQLite)
.sqlite                             # specific sqlite files
db.sqlite                           # specific sqlite files
/// .env also handles connection strings for Prisma
# Prisma client generated by `prisma generate`
# This might be important to keep in git if multiple developers work on it
# but often it's regenerated locally. If you regenerate, uncomment:
# node_modules/.prisma/client/


# Uploads
# ========================
# Directory for temporary file uploads (e.g., Multer destination)
/uploads/
# If you want to keep the 'uploads' directory but ignore its contents:
#/uploads/*
#!uploads/.gitkeep # Optionally, keep an empty .gitkeep file to track the folder itself

# Cloudinary
# ========================
# Cloudinary credentials are in .env, which is already ignored.
# No other specific files to ignore for Cloudinary.

# Express
# ========================
# No specific files to ignore for Express beyond common Node.js patterns.

# Docker
# ========================
.dockerignore
Dockerfile.production.local # if you have a local dev dockerfile
.dockerdata/                # example of local docker volumes


# Custom for eventFlow (if any specific generated files)
# ========================
# Add any other files specific to your project that should be ignored.